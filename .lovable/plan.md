

## Plan: Add Google Analytics Tag

**What**: Insert the Google Analytics (gtag.js) snippet into `index.html`, replacing the existing retargeting pixel placeholder comment.

**Changes**:
- **`index.html`**: Add the two Google tag `<script>` elements (async loader + config for `G-57H1Q82XZ4`) in the `<head>`, replacing the `<!-- Retargeting Pixel Placeholder -->` comment.

Single file, ~6 lines added.

